class Account_holder:
    def __init__(self, name, age, balance, accno):
        self.name = name
        self.age = int(age)
        self.balance = float(balance)
        self.accno = int(accno)
